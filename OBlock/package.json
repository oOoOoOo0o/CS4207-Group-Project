{
  "name": "oblock",
  "version": "1.0.0",
  "directories": {
    "test": "test"
  },
  "main": "src",
  "dependencies": {
    "express": "^4.21.2",
    "forever": "^4.0.3",
    "truffle": "^5.11.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "forever start src/server.js && echo Started server on http://localhost:3000 ",
    "shutdown": "forever stop src/server.js"
  }
}
